{
  "name": "notebook",
  "version": "1.0.0",
  "description": "My Online Notebook",
  "main": "index.js",
  "scripts": {
    "build": "gitbook install && gitbook build",
    "preview": "gitbook serve",
    "publish": "npm run build && gulp publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:ufonion/notebook.git"
  },
  "author": "ufonion",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-gh-pages": "^0.5.4",
    "gulp-load-plugins": "^2.0.6"
  }
}
